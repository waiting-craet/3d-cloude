# 实施计划

- [x] 1. 编写bug条件探索测试
  - **Property 1: Fault Condition** - CSV中文列名识别失败
  - **重要**: 在实施修复之前编写此基于属性的测试
  - **目标**: 展示bug存在的反例，确认当前代码无法识别中文列名
  - **作用域PBT方法**: 针对包含中文列名的具体失败案例进行测试
  - 测试当CSV包含中文列名（"源节点"、"目标节点"、"关系"等）时，parseCSVWithEdgeData函数抛出错误
  - 在未修复的代码上运行测试 - 预期失败（这确认了bug的存在）
  - 记录发现的反例（例如："包含['源节点', '目标节点', '关系']的CSV抛出'CSV文件必须包含source和target列'错误"）
  - 测试实现细节来自设计文档中的Fault Condition规范
  - 测试断言应匹配设计文档中的Expected Behavior Properties
  - **预期结果**: 测试失败（这是正确的 - 证明bug存在）
  - 当测试编写完成、运行并记录失败时，标记任务完成
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [x] 2. 编写保持性属性测试（在实施修复之前）
  - **Property 2: Preservation** - 英文列名继续正常工作
  - **重要**: 遵循观察优先方法
  - 在未修复的代码上观察非bug输入（英文列名）的行为
  - 编写基于属性的测试，捕获来自Preservation Requirements的观察到的行为模式
  - 基于属性的测试生成大量测试用例以提供更强的保证
  - 在未修复的代码上运行测试
  - **预期结果**: 测试通过（这确认了要保持的基线行为）
  - 当测试编写完成、运行并在未修复代码上通过时，标记任务完成
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. 修复CSV中文列名识别问题

  - [x] 3.1 实施修复
    - 在`lib/services/graph-import.ts`的`parseCSVWithEdgeData`函数中扩展列名识别数组
    - 扩展source列名数组（第426行）：添加`'源节点'`, `'起点'`, `'来源'`
    - 扩展target列名数组（第427行）：添加`'目标节点'`, `'终点'`, `'目的地'`
    - 扩展label列名数组（第428行）：添加`'关系'`, `'关系类型'`, `'边类型'`, `'连接类型'`
    - _Bug_Condition: isBugCondition(input) where input.headers包含中文列名["源节点", "起点", "来源", "目标节点", "终点", "目的地", "关系", "关系类型", "边类型", "连接类型"]_
    - _Expected_Behavior: 系统能够正确识别中文列名并成功解析CSV数据，创建正确的节点和边（来自设计文档）_
    - _Preservation: 所有使用英文列名的CSV文件继续正常工作，保持现有的解析行为、错误处理和数据结构（来自设计文档）_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

  - [x] 3.2 验证bug条件探索测试现在通过
    - **Property 1: Expected Behavior** - CSV中文列名识别成功
    - **重要**: 重新运行任务1中的相同测试 - 不要编写新测试
    - 任务1中的测试编码了预期行为
    - 当此测试通过时，确认预期行为得到满足
    - 运行步骤1中的bug条件探索测试
    - **预期结果**: 测试通过（确认bug已修复）
    - _Requirements: Expected Behavior Properties from design_

  - [x] 3.3 验证保持性测试仍然通过
    - **Property 2: Preservation** - 英文列名继续正常工作
    - **重要**: 重新运行任务2中的相同测试 - 不要编写新测试
    - 运行步骤2中的保持性属性测试
    - **预期结果**: 测试通过（确认没有回归）
    - 确认修复后所有测试仍然通过（没有回归）

- [x] 4. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户
