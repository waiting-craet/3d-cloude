# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - AI Analysis Errors (Duplicate Detection, API 500, Async Listener)
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the three bugs exist
  - **Scoped PBT Approach**: Scope the property to concrete failing cases: (1) duplicate detection database error, (2) API 500 error, (3) async listener error
  - Test that AI analysis fails with proper error handling for all three error conditions (from Fault Condition in design)
  - Simulate duplicate detection database connection failure → expect generic error message on unfixed code
  - Simulate API route unhandled exception → expect 500 status on unfixed code
  - Simulate component unmount during analysis → expect async listener error on unfixed code
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Duplicate detection throws generic "Failed to check for duplicates" without specific error details
    - API returns 500 without proper error logging or user-friendly messages
    - Async operations throw unhandled promise rejections with Chrome extension message channel errors
    - Multiple PrismaClient instances may exhaust connection pool
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing AI Analysis Functionality
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for successful AI analysis flows (non-buggy inputs)
  - Observe: AI analysis without graphId (no duplicate detection) works correctly on unfixed code
  - Observe: Successful duplicate detection marks duplicates correctly on unfixed code
  - Observe: Preview modal displays generated nodes and edges correctly on unfixed code
  - Observe: Cancellation using AbortController works correctly on unfixed code
  - Observe: Retry functionality attempts analysis again on unfixed code
  - Observe: Network error handling displays appropriate messages on unfixed code
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix for duplicate AI analysis errors

  - [x] 3.1 Implement Prisma client singleton pattern
    - Replace `new PrismaClient()` in API routes with singleton instance
    - Create or import shared Prisma client from `lib/db-helpers.ts` or similar
    - Ensure proper connection management and prevent connection pool exhaustion
    - _Bug_Condition: isBugCondition(input) where input.duplicateDetectionFails due to multiple PrismaClient instances_
    - _Expected_Behavior: Prisma client properly initialized and shared across requests_
    - _Preservation: Existing database queries continue to work correctly_
    - _Requirements: 2.1, 2.4_

  - [x] 3.2 Enhance error handling in AI analysis API route
    - Add specific handling for Prisma connection errors in duplicate detection try-catch
    - Add specific handling for query timeout errors
    - Log detailed error information for debugging (error type, stack trace, context)
    - Return more specific error messages based on error type (connection vs timeout vs query)
    - Consider graceful degradation: allow analysis to proceed without duplicate detection on error
    - _Bug_Condition: isBugCondition(input) where input.apiReturns500Error due to unhandled exceptions_
    - _Expected_Behavior: API returns 200 with proper error handling or specific error status with user-friendly messages_
    - _Preservation: Successful API responses continue to work correctly_
    - _Requirements: 2.2, 2.4, 2.5_

  - [x] 3.3 Fix async listener error in handleAIAnalysis
    - Add useEffect cleanup to cancel pending requests on component unmount
    - Add mounted ref to prevent state updates after unmount
    - Ensure all state updates (setIsAnalyzing, setAnalysisError) check if component is mounted
    - Add proper cleanup in catch blocks for AbortController signal
    - Wrap fetch calls in additional try-catch for Chrome extension compatibility
    - Add .catch() handlers to all async operations to prevent unhandled promise rejections
    - _Bug_Condition: isBugCondition(input) where input.asyncListenerErrorOccurs at page.tsx:219_
    - _Expected_Behavior: No async listener errors, proper cleanup on unmount, no unhandled promise rejections_
    - _Preservation: Existing async operations and state management continue to work correctly_
    - _Requirements: 2.3, 2.4_

  - [x] 3.4 Add connection validation in duplicate detection service
    - Add Prisma client connection checks before queries in fetchExistingNodes and fetchExistingEdges
    - Add retry logic for transient connection failures
    - Return empty arrays on connection failure instead of throwing (graceful degradation)
    - Add explicit timeout handling for database queries
    - Log timeout occurrences for monitoring
    - _Bug_Condition: isBugCondition(input) where input.duplicateDetectionFails due to database connection issues_
    - _Expected_Behavior: Duplicate detection handles connection failures gracefully with fallback behavior_
    - _Preservation: Successful duplicate detection queries continue to work correctly_
    - _Requirements: 2.1, 2.4_

  - [x] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - AI Analysis Success with Proper Error Handling
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Verify that duplicate detection errors are handled gracefully with user-friendly messages
    - Verify that API errors return proper status codes with detailed logging
    - Verify that async listener errors do not occur with proper cleanup
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing AI Analysis Functionality
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify AI analysis without graphId still works correctly
    - Verify successful duplicate detection still marks duplicates correctly
    - Verify preview modal still displays correctly
    - Verify cancellation still works correctly
    - Verify retry functionality still works correctly
    - Verify network error handling still works correctly
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
