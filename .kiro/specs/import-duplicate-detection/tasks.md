# 实现计划: 导入冗余数据检测

## 概述

本功能为导入数据页面添加冗余数据检测能力。实现将分为5个阶段:核心检测服务、API集成、UI更新、属性测试和性能验证。所有代码使用 TypeScript 实现。

## 任务

- [x] 1. 创建冗余检测服务核心模块
  - [x] 1.1 创建 duplicate-detection.ts 服务文件和类型定义
    - 在 `lib/services/duplicate-detection.ts` 创建服务文件
    - 定义 `DuplicateDetectionResult` 和 `FilteredData` 接口
    - 定义节点和边的数据类型
    - _需求: 1.1, 1.2_

  - [x] 1.2 实现节点冗余检测函数
    - 实现 `detectDuplicateNodes` 函数
    - 使用 Set 数据结构进行 O(1) 查找
    - 基于节点的 name 字段判断冗余
    - _需求: 1.1, 1.3_

  - [ ]* 1.3 为节点冗余检测编写属性测试
    - **属性 1: 节点冗余检测准确性**
    - **验证需求: 1.1, 1.3**
    - 生成随机节点集合,部分节点 name 重复
    - 验证所有 name 重复的节点被标记为冗余
    - 标签: `Feature: import-duplicate-detection, Property 1: 节点冗余检测准确性`

  - [x] 1.4 实现边冗余检测函数
    - 实现 `detectDuplicateEdges` 函数
    - 使用唯一键格式: `${sourceName}|${targetName}|${label}`
    - 基于源节点、目标节点和关系类型判断冗余
    - _需求: 1.2, 1.4_

  - [ ]* 1.5 为边冗余检测编写属性测试
    - **属性 2: 边冗余检测准确性**
    - **验证需求: 1.2, 1.4**
    - 生成随机边集合,部分边的源、目标、类型组合重复
    - 验证所有组合重复的边被标记为冗余
    - 标签: `Feature: import-duplicate-detection, Property 2: 边冗余检测准确性`

  - [x] 1.6 实现数据过滤函数
    - 实现 `filterNonDuplicateData` 函数
    - 从原始数据中移除冗余项
    - 返回过滤后的数据和统计信息
    - _需求: 2.1, 2.2_

  - [ ]* 1.7 编写核心函数的单元测试
    - 测试完全冗余的数据集
    - 测试部分冗余的混合数据
    - 测试无冗余的全新数据
    - 测试空数据集
    - 测试边界情况(缺少字段等)

- [x] 2. 实现完整的冗余检测流程
  - [x] 2.1 实现数据库查询逻辑
    - 实现查询现有节点的函数(只选择 name 字段)
    - 实现查询现有边的函数(选择 label, fromNode.name, toNode.name)
    - 使用 Prisma 的 select 优化查询性能
    - _需求: 1.1, 1.2, 4.3_

  - [x] 2.2 实现主检测流程函数
    - 实现 `detectAndFilterDuplicates` 函数
    - 集成数据库查询和冗余检测逻辑
    - 返回检测结果和过滤后的数据
    - _需求: 1.5, 2.1, 2.2_

  - [ ]* 2.3 为检测时序编写属性测试
    - **属性 3: 检测时序正确性**
    - **验证需求: 1.5**
    - 验证检测总是在数据库写入之前执行
    - 标签: `Feature: import-duplicate-detection, Property 3: 检测时序正确性`

  - [ ]* 2.4 为数据导入完整性编写属性测试
    - **属性 4: 非冗余数据导入完整性**
    - **验证需求: 2.1, 2.2**
    - 生成包含冗余和非冗余数据的随机集合
    - 验证导入的数据恰好是非冗余数据
    - 标签: `Feature: import-duplicate-detection, Property 4: 非冗余数据导入完整性`

  - [ ]* 2.5 为全冗余数据处理编写属性测试
    - **属性 5: 全冗余数据处理**
    - **验证需求: 2.4**
    - 生成完全冗余的数据集
    - 验证导入成功但无新数据写入
    - 标签: `Feature: import-duplicate-detection, Property 5: 全冗余数据处理`

- [x] 3. 检查点 - 核心服务验证
  - 确保所有核心检测函数通过单元测试
  - 确认数据库查询逻辑正确
  - 如有问题请询问用户

- [x] 4. 集成冗余检测到导入 API
  - [x] 4.1 修改 /api/import 路由添加冗余检测
    - 在数据验证之后、布局生成之前调用 `detectAndFilterDuplicates`
    - 使用过滤后的数据进行布局生成和导入
    - 保持原有的导入流程和事务逻辑
    - _需求: 1.5, 2.3_

  - [x] 4.2 更新 API 返回结果格式
    - 添加 `duplicateNodesCount` 字段
    - 添加 `duplicateEdgesCount` 字段
    - 添加 `totalNodesInFile` 字段
    - 添加 `totalEdgesInFile` 字段
    - _需求: 3.2, 3.3, 3.4, 3.5_

  - [ ]* 4.3 为统计信息一致性编写属性测试
    - **属性 6: 统计信息一致性**
    - **验证需求: 3.2, 3.3, 3.4, 3.5**
    - 生成随机数据集
    - 验证: 冗余数 + 导入数 = 总数
    - 标签: `Feature: import-duplicate-detection, Property 6: 统计信息一致性`

  - [x] 4.4 添加错误处理逻辑
    - 捕获数据库查询错误并返回描述性消息
    - 捕获数据格式错误并返回描述性消息
    - 记录错误日志(包含错误类型、消息、上下文)
    - 发生错误时中止导入操作
    - _需求: 5.1, 5.2, 5.3, 5.4_

  - [ ]* 4.5 为错误处理编写属性测试
    - **属性 7: 错误处理完整性**
    - **验证需求: 5.1, 5.2, 5.3**
    - 生成触发各种错误的场景
    - 验证错误被捕获、消息被显示、操作被中止
    - 标签: `Feature: import-duplicate-detection, Property 7: 错误处理完整性`

  - [ ]* 4.6 为错误日志记录编写属性测试
    - **属性 8: 错误日志记录**
    - **验证需求: 5.4**
    - 生成触发各种错误的场景
    - 验证日志被正确记录(包含类型、消息、上下文)
    - 标签: `Feature: import-duplicate-detection, Property 8: 错误日志记录`

  - [ ]* 4.7 编写 API 集成测试
    - 测试完整的导入流程(包含冗余检测)
    - 测试多次导入相同数据(验证冗余检测)
    - 测试跨项目导入(验证不会误判为冗余)
    - 测试跨图谱导入(验证不会误判为冗余)

- [x] 5. 检查点 - API 集成验证
  - 确保 API 正确调用冗余检测服务
  - 确认返回结果包含所有统计信息
  - 确认错误处理逻辑正常工作
  - 如有问题请询问用户

- [x] 6. 更新导入页面 UI
  - [x] 6.1 添加统计信息状态管理
    - 在 `app/import/page.tsx` 添加 `importStats` 状态
    - 定义统计信息的类型接口
    - 在 `handleUpload` 函数中更新统计信息
    - _需求: 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 6.2 更新导入进度弹窗 UI
    - 在加载模态框中添加统计信息显示区域
    - 显示冗余节点数量
    - 显示冗余边数量
    - 显示导入节点数量
    - 显示导入边数量
    - 使用清晰易读的格式展示信息
    - _需求: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

  - [x] 6.3 优化错误消息显示
    - 显示冗余检测相关的错误消息
    - 确保错误消息清晰描述问题
    - 保持与现有错误处理的一致性
    - _需求: 5.1, 5.2_

  - [ ]* 6.4 编写 UI 组件测试
    - 测试统计信息正确显示在进度弹窗中
    - 测试冗余数据过滤后的成功消息
    - 测试全冗余数据的提示消息
    - 测试错误情况下的错误消息显示

- [x] 7. 检查点 - UI 更新验证
  - 确保统计信息正确显示
  - 确认 UI 布局和样式符合设计
  - 测试各种数据场景下的 UI 表现
  - 如有问题请询问用户

- [x] 8. 性能优化和验证
  - [x] 8.1 验证数据结构性能
    - 确认使用 Set 进行冗余检查
    - 确认使用 Map 存储节点映射
    - 确认批量查询避免 N+1 问题
    - _需求: 4.3_

  - [x] 8.2 优化数据库查询
    - 确认查询只选择必要字段
    - 验证数据库索引存在(name, label 字段)
    - 确认查询次数固定(2次: 节点+边)
    - _需求: 4.1, 4.2, 4.3_

  - [ ]* 8.3 手动性能测试
    - 测试 1000 个节点的检测时间(目标: <2秒)
    - 测试 5000 条边的检测时间(目标: <3秒)
    - 监控内存使用情况(目标: <100MB)
    - 记录性能测试结果

- [x] 9. 最终检查点
  - 确保所有测试通过
  - 确认所有需求都已实现
  - 验证错误处理和日志记录
  - 确认性能符合要求
  - 如有问题请询问用户

## 注意事项

- 标记 `*` 的任务为可选任务,可以跳过以加快 MVP 开发
- 每个任务都引用了具体的需求编号以确保可追溯性
- 属性测试使用 fast-check 库,每个测试至少运行 100 次迭代
- 检查点任务确保增量验证,及时发现问题
- 所有代码使用 TypeScript 实现,确保类型安全
