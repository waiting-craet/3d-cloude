# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Console Error Detection
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bugs exist
  - **Scoped PBT Approach**: Scope the property to concrete failing cases - application load scenarios that trigger 404s, TypeErrors, and HMR removeChild errors
  - Test that application load produces console errors for non-existent resources (from Fault Condition in design)
  - Test that CSS module hot reload produces removeChild errors (from Fault Condition in design)
  - Test implementation details:
    - Load application and capture console errors
    - Monitor network tab for 404 requests to "hybridaction/zybTrackerStatisticsAction"
    - Monitor network tab for 404 requests to hashed JS files like "main.7ee886d8.js"
    - Monitor console for "v[w] is not a function" TypeError
    - Make CSS module changes and capture removeChild errors during HMR
  - The test assertions should match the Expected Behavior Properties from design:
    - Assert consoleErrors.length > 0 (will be 0 after fix)
    - Assert networkRequests contains 404 responses (will be none after fix)
    - Assert hmrReloads contain removeChild errors (will be successful after fix)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bugs exist)
  - Document counterexamples found:
    - Specific 404 URLs encountered
    - Exact TypeError messages
    - Number and timing of removeChild errors
    - Browser and extension configuration
  - Mark task complete when test is written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing Functionality Preservation
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy scenarios:
    - HMR works for non-CSS file changes (e.g., TypeScript, JSX)
    - CSS modules apply scoped styles correctly to components
    - Production builds generate optimized bundles with proper hashing
    - Application functions correctly during normal user interactions
    - Development server auto-reloads on file changes
    - Third-party libraries load and execute correctly
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Test HMR functionality for various file types (JS, TS, JSX, TSX)
    - Test CSS module scoping across multiple components
    - Test production build output structure and file hashing
    - Test runtime functionality for core application features
    - Test auto-reload behavior for different file change scenarios
    - Test third-party library loading and initialization
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix console errors through systematic investigation and targeted corrections

  - [x] 3.1 Investigation Phase - Identify root causes
    - Clear all build caches to eliminate stale references
      - Delete `.next` directory completely
      - Clear browser cache and hard reload
      - Delete `node_modules/.cache` if it exists
      - Verify no references to "main.7ee886d8.js" remain after rebuild
    - Identify source of "hybridaction/zybTrackerStatisticsAction" requests
      - Open browser DevTools Network tab and inspect request initiator
      - Check browser extensions and disable them one by one
      - Search codebase for "hybridaction" or "zybTracker" references
      - Inspect HTML output for injected scripts
    - Investigate CSS module hot reload failures
      - Identify which CSS modules trigger removeChild errors
      - Check for custom portal/modal implementations that manipulate DOM
      - Review component lifecycle methods that interact with DOM nodes
      - Test HMR behavior with minimal CSS module changes
    - Review Next.js configuration
      - Examine `next.config.js` for missing HMR settings
      - Check webpack configuration for proper module handling
      - Verify static asset configuration
      - Compare with Next.js best practices documentation
    - Document findings for each investigation area
    - _Bug_Condition: isBugCondition(input) where input.hasNonExistentResourceRequests = true OR input.hasTypeErrorInLoadedScripts = true OR input.hasHMRRemoveChildErrors = true_
    - _Expected_Behavior: result.consoleErrors.length = 0 AND result.networkRequests.all(r => r.status != 404) AND result.hmrReloads.all(r => r.success = true)_
    - _Preservation: HMR functionality, CSS module scoping, production builds, runtime behavior, auto-reload, third-party library loading from Preservation Requirements_
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.2 Fix Phase - Apply targeted corrections based on investigation findings
    - If stale cache identified: Implement automated cache clearing
      - Add npm script for clean rebuild: `"clean": "rm -rf .next"`
      - Document cache clearing procedure for developers
      - Verify `.next` is in `.gitignore`
    - If external script identified: Remove or block external interference
      - Remove any analytics code referencing non-existent endpoints
      - Add Content Security Policy headers to block unwanted scripts
      - Document browser extension conflicts for team
    - If HMR race condition identified: Fix CSS module hot reload handling
      - Add null checks before DOM manipulation in HMR code
      - Implement proper cleanup in portal/modal components
      - Configure Next.js to handle CSS modules more gracefully
      - Add error boundaries around HMR-sensitive components
    - If configuration gap identified: Enhance Next.js configuration
      - Add proper webpack configuration for HMR
      - Configure build optimization settings
      - Set up proper static asset handling
      - Enable React Strict Mode for better error detection
    - _Bug_Condition: isBugCondition(input) from design_
    - _Expected_Behavior: expectedBehavior(result) from design - zero console errors, no 404s, successful HMR_
    - _Preservation: Preservation Requirements from design - all existing functionality unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Console Error Elimination
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bugs are fixed)
    - Verify:
      - No console errors during application load
      - No 404 network requests
      - No TypeError during JavaScript execution
      - No removeChild errors during CSS module hot reload
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing Functionality Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation tests still pass after fix:
      - HMR functionality for all file types
      - CSS module scoping
      - Production build generation
      - Runtime functionality
      - Auto-reload behavior
      - Third-party library loading
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all tests to verify complete fix
  - Verify zero console errors in development mode
  - Verify zero console errors in production build
  - Verify clean network tab with no 404 requests
  - Verify successful HMR reloads for all file types
  - Ask the user if questions arise
