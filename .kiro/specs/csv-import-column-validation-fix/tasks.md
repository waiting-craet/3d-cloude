# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - CSV列名规范化失败导致匹配错误
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Test concrete failing cases with BOM, extra whitespace, invisible characters, and case variations
  - Test implementation details from Fault Condition in design:
    - CSV with BOM marker (\uFEFF) at start: `"\uFEFFSource,Target,Label"`
    - CSV with extra whitespace: `" Source , Target , Label "`
    - CSV with invisible characters: `"Source\u200B,Target\u200B,Label"`
    - CSV with uppercase variations: `"SOURCE,TARGET,LABEL"`
    - CSV with mixed case aliases: `"FROM,TO,Type"`
  - The test assertions should match the Expected Behavior Properties from design:
    - Assert parseCSVFile successfully parses the file
    - Assert result.nodes.length > 0
    - Assert result.edges.length > 0
    - Assert no error is thrown
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - 标准CSV解析行为保持不变
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Standard lowercase column names: `"source,target,label"`
    - Supported aliases: `"from,to,relationship"`, `"src,dst,type"`
    - Node data format with coordinates: CSV containing `x,y` columns
    - Empty lines handling: CSV with blank lines between data rows
    - Special characters in data: Node names with commas, quotes, unicode
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - For all standard format CSV (lowercase, no special chars), parsing result matches original behavior
    - For all alias variations (from/to, src/dst), correct column identification continues
    - For all node data CSV (with x,y), parseCSVWithNodeData is used
    - For all CSV with empty lines, lines are correctly skipped
    - For all CSV with special chars in values, data is correctly preserved
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for CSV列名大小写和特殊字符匹配问题

  - [x] 3.1 Create normalizeColumnName helper function
    - Add new function in `lib/services/graph-import.ts`
    - Remove BOM marker (\uFEFF) from start of string
    - Remove all invisible Unicode characters (zero-width spaces: \u200B, \u200C, \u200D)
    - Convert to lowercase using .toLowerCase()
    - Trim leading and trailing whitespace
    - Replace multiple internal spaces with single space
    - _Bug_Condition: isBugCondition(input) where input has BOM, extra whitespace, invisible chars, or case variations_
    - _Expected_Behavior: normalizeColumnName returns clean lowercase string for all inputs_
    - _Preservation: Standard strings without special chars return same result as trim().toLowerCase()_
    - _Requirements: 2.1, 2.2_

  - [x] 3.2 Update parseCSVFile to use normalizeColumnName
    - Replace `headers = lines[0].split(',').map(h => h.trim().toLowerCase())` with `headers = lines[0].split(',').map(h => normalizeColumnName(h))`
    - Ensure headers array is properly normalized before column detection
    - _Bug_Condition: Headers with special chars or case variations_
    - _Expected_Behavior: All valid column name variations are correctly identified_
    - _Preservation: Standard headers continue to work exactly as before_
    - _Requirements: 2.1, 2.2, 3.1_

  - [x] 3.3 Improve error message in parseCSVWithEdgeData
    - Update error message when sourceIndex === -1 or targetIndex === -1
    - Include actual found column names in error: `CSV文件必须包含source和target列。找到的列：${headers.join(', ')}`
    - Help users diagnose column name issues
    - _Bug_Condition: Column matching fails_
    - _Expected_Behavior: Error message shows actual column names found_
    - _Preservation: Error is still thrown when columns are truly missing_
    - _Requirements: 2.3_

  - [x] 3.4 Add unit tests for normalizeColumnName
    - Test BOM removal: `normalizeColumnName('\uFEFFSource')` returns `'source'`
    - Test whitespace handling: `normalizeColumnName(' Source ')` returns `'source'`
    - Test invisible chars: `normalizeColumnName('Source\u200B')` returns `'source'`
    - Test case conversion: `normalizeColumnName('SOURCE')` returns `'source'`
    - Test multiple spaces: `normalizeColumnName('Source  Name')` returns `'source name'`
    - Test standard input: `normalizeColumnName('source')` returns `'source'`
    - _Requirements: 2.1, 2.2_

  - [x] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - CSV列名规范化成功
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify all edge cases now parse successfully:
      - BOM marker CSV parses correctly
      - Extra whitespace CSV parses correctly
      - Invisible characters CSV parses correctly
      - Case variations CSV parses correctly
      - Mixed case aliases CSV parses correctly
    - _Requirements: 2.1, 2.2_

  - [x] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - 标准CSV解析行为保持不变
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix:
      - Standard lowercase column names continue to work
      - Alias support continues to work
      - Node data format continues to use parseCSVWithNodeData
      - Empty lines continue to be skipped
      - Special characters in data continue to be preserved
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all unit tests for normalizeColumnName
  - Run bug condition exploration test (Property 1) - should PASS
  - Run preservation property tests (Property 2) - should PASS
  - Verify error messages include actual column names
  - Test with real CSV files containing various edge cases
  - Ensure all tests pass, ask the user if questions arise
